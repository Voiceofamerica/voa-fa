query Search($query: String!, $zoneId: Int) {
  search(source: fa, keywords: $query, zoneId: $zoneId) {
    id
    title
    introduction
    pubDate
    image {
      url
    }
    video {
      url
    }
    audio {
      url
    }
  }
}
