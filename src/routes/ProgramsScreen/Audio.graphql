query ProgramAudio($source: Audience!) {
  content(source: $source, type: [Clip]) {
    id
    pubDate
    image {
      tiny
      hero
    }
    audio {
      url
      audioTitle
      audioDescription
    }
  }
}
