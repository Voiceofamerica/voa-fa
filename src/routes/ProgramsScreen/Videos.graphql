query ProgramVideos($zone: Int) {
  program(source: fa, type: [Video], zoneId: $zone) {
    id
    date
    timeLeft
    programTitle
    programDescription
    image {
      tiny
    }
    url
  }
}
