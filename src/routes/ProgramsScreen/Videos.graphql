query ProgramVideos($source: Audience!, $zone: Int) {
  program(source: $source, type: [Video], zoneId: $zone) {
    id
    date
    timeLeft
    programTitle
    programDescription
    image {
      tiny
    }
    url
  }
}
